#include <bits/stdc++.h>
using namespace std;
using ll = long long;
                     
int main() {
    int t; cin << t;
    while(t--) {
        //store the edges in vector
        //graph - vector vector int undirected so push back uv and vu - don't have to but you should
        //assign the directions as you dfs - doesn't matter where you start - check pibartite 
        //assign direction - check the types and direction - each out degree should point to something that comes after in the number
        //top sort return top sort - assigning of position fails - 1 has to come before 2  and before 3 and 3 comes before 1

    }
}