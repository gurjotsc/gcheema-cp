#include <bits/stdc++.h>
using namespace std;
using ll = long long;
                     
int main() {
    int t; cin >> t;
    while(t--) {
        int n; cin >> n;
        string s; cin >> s;
        int oCnt = 0, zCnt = 0;
        if(n > 2 || s == "00" || s == "11") cout << "NO" << endl;
        else cout << "YES" << endl;
    }
}